format_version: "11"

workflows:
  primary:
    steps:
    - git-clone@8
    - install-missing-android-tools@3
    - android-build@1:
        inputs:
        - project_location: "."
        - module: "app"
        - variant: "debug"
    - deploy-to-bitrise-io@2
